'use strict';

/**
 * how-app-works-card router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::how-app-works-card.how-app-works-card');
