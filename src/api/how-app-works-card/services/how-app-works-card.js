'use strict';

/**
 * how-app-works-card service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::how-app-works-card.how-app-works-card');
