'use strict';

/**
 * biomarker-card controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::biomarker-card.biomarker-card');
